% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types.R
\name{px_terrestrial}
\alias{px_terrestrial}
\title{Create a terrestrial laser scanning point cloud}
\usage{
px_terrestrial(
  xyz,
  intensity = NULL,
  classification = NULL,
  rgb = NULL,
  normals = NULL,
  return_number = NULL,
  number_of_returns = NULL,
  scan_positions = NULL,
  metadata = list()
)
}
\arguments{
\item{xyz}{Numeric matrix with 3 columns (x, y, z).}

\item{intensity}{Numeric vector of intensity values, or \code{NULL}.}

\item{classification}{Integer vector of ASPRS classification codes, or \code{NULL}.}

\item{rgb}{Integer matrix with 3 columns (R, G, B), or \code{NULL}.}

\item{normals}{Numeric matrix with 3 columns (nx, ny, nz), or \code{NULL}.}

\item{return_number}{Integer vector of return numbers, or \code{NULL}.}

\item{number_of_returns}{Integer vector of total returns, or \code{NULL}.}

\item{scan_positions}{List of \code{px_scan_position} objects, or \code{NULL}.}

\item{metadata}{Named list of acquisition metadata.}
}
\value{
An object of class \code{c("px_terrestrial", "px_cloud")}.
}
\description{
Constructor for TLS data. Sets platform to "terrestrial" and provides
appropriate defaults for registration, ground classification, and
feature extraction. Scan positions are first-class metadata.
}
\examples{
xyz <- matrix(rnorm(300), ncol = 3)
pos <- px_scan_position(0, 0, 1.5, scan_id = "setup1")
cloud <- px_terrestrial(xyz, scan_positions = list(pos))

}
