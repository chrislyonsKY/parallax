.onLoad <- function(libname, pkgname) {
  # TODO: check Rust shared library loaded correctly
  # TODO: check optional deps and inform user
  # if (!requireNamespace("lidR", quietly = TRUE)) {
  #   packageStartupMessage("lidR not installed. from_lidr()/to_lidr() unavailable.")
  # }
}
