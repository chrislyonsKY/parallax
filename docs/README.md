This directory is reserved for the generated pkgdown site.

Local build:

```r
pkgdown::build_site()
```

Automated deployment is handled by the pkgdown workflow once the repository
is pushed to GitHub.
